#!/bin/bash
cd /usr/bin
chmod +x add-host
chmod +x dd-host.sh
chmod +x add-on
chmod +x add-on.sh
chmod +x addssh
chmod +x add-trojan
chmod +x add-trojan-gfw
chmod +x add-vless
chmod +x add-vmess
chmod +x backup
chmod +x backup.sh
chmod +x bashrock
chmod +x cek-service
chmod +x cek-service.sh
chmod +x cek-ssh
chmod +x cek-ssh.sh
chmod +x cek
chmod +x cek-trojan
chmod +x cek-trojan.sh
chmod +x cek-vless
chmod +x cek-vless.sh
chmod +x cek-vmess
chmod +x cek-vmess.sh
chmod +x cert
chmod +x cert.sh
chmod +x clear-log
chmod +x clear-log.sh
chmod +x delexp
chmod +x delexp.sh
chmod +x delssh
chmod +x delssh.sh
chmod +x del-trojan
chmod +x del-trojan.sh
chmod +x del-trojan-trial
chmod +x del-trojan-trial.sh
chmod +x del-vless
chmod +x del-vless.sh
chmod +x del-vless-trial
chmod +x del-vless-trial.sh
chmod +x del-vmess
chmod +x del-vmess.sh
chmod +x del-vmess-trial
chmod +x del-vmess-trial.sh
chmod +x disable-trojan
chmod +x disable-trojan.sh
chmod +x disable-vless
chmod +x disable-vless.sh
chmod +x disable-vmess
chmod +x disable-vmess.sh
chmod +x limit
chmod +x limit-ip-ssh
chmod +x limit-ip-ssh.sh
chmod +x limit-ip-trojan
chmod +x limit-ip-trojan.sh
chmod +x limit-ip-vless
chmod +x limit-ip-vless.sh
chmod +x limit-ip-vmess
chmod +x limit-ip-vmess.sh
chmod +x limit-quota-trojan
chmod +x limit-quota-trojan.sh
chmod +x limit-quota-vless
chmod +x limit-quota-vless.sh
chmod +x limit-quota-vmess
chmod +x limit-quota-vmess.sh
chmod +x limit.sh
chmod +x list-ssh
chmod +x list-ssh.sh
chmod +x list-trojan
chmod +x list-trojan.sh
chmod +x list-vless
chmod +x list-vless.sh
chmod +x list-vmess
chmod +x list-vmess.sh
chmod +x log-ssh
chmod +x log-ssh.sh
chmod +x log-trojan
chmod +x log-trojan.sh
chmod +x log-vless
chmod +x log-vless.sh
chmod +x log-vmess
chmod +x log-vmess.sh
chmod +x loop
chmod +x loop.sh
chmod +x matikan
chmod +x matikan.sh
chmod +x menu-add-on
chmod +x menu-add-on.sh
chmod +x menu-ssh
chmod +x menu-ssh.sh
chmod +x menu-system
chmod +x menu-system.sh
chmod +x menu-trojan
chmod +x menu-trojan.sh
chmod +x menu-vless
chmod +x menu-vless.sh
chmod +x menu-vmess
chmod +x menu-vmess.sh
chmod +x mesin
chmod +x mesinssh
chmod +x mesinssh.sh
chmod +x quota
chmod +x quota.sh
chmod +x quota-trojan-grpc
chmod +x quota-trojan-grpc.sh
chmod +x quota-trojan-tcp
chmod +x quota-trojan-tcp.sh
chmod +x quota-trojan-ws
chmod +x quota-trojan-ws.sh
chmod +x quota-vless-grpc
chmod +x quota-vless-grpc.sh
chmod +x quota-vless-ws
chmod +x quota-vless-ws.sh
chmod +x quota-vmess-grpc
chmod +x quota-vmess-grpc.sh
chmod +x quota-vmess-ws
chmod +x quota-vmess-ws-orbit
chmod +x quota-vmess-ws-orbit1
chmod +x quota-vmess-ws-orbit1.sh
chmod +x quota-vmess-ws-orbit.sh
chmod +x quota-vmess-ws.sh
chmod +x renewsshprem
chmod +x renewsshprem.sh
chmod +x renew-trojan-prem
chmod +x renew-trojan-prem.sh
chmod +x renew-vless-prem
chmod +x renew-vless-prem.sh
chmod +x renew-vmess-prem
chmod +x renew-vmess-prem.sh
chmod +x speedtest
chmod +x switch-trojan
chmod +x switch-trojan.sh
chmod +x trial-trojan
chmod +x trial-trojan.sh
chmod +x trial-vless
chmod +x trial-vless.sh
chmod +x trial-vmess
chmod +x trial-vmess.sh
chmod +x unban-akun
chmod +x unban-akun.sh
chmod +x update.sh
chmod +x user
chmod +x user.sh
chmod +x xp
chmod +x xp.sh

